import 'package:flutter/material.dart';

class CustomColors {
  static const background = Color.fromARGB(255, 25, 25, 25);
  static const secondaryColor = Color.fromARGB(255, 120, 70, 190);
  // static const secondaryColor = Colors.deepPurple;

  static const navigationBarBackground = Color.fromARGB(255, 18, 18, 18);
  static const alarmCardColor = Color.fromARGB(150, 35, 35, 40);
  static const screensFontColor = Color.fromARGB(200, 255, 255, 255);
}

class GradientColors {
  static const linearGrad = LinearGradient(
    colors: [Colors.purpleAccent, Colors.tealAccent, Colors.blueAccent],
  );
}
